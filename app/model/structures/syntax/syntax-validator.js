/**
 * Создаёт объект поля, для проверки синтаксиса
 */
export default class SyntaxValidator {
    pattern
    constructor(pattern) {
        this.pattern = pattern
    }
    /**
     * Проверяет удовлетворяет ли 
     */
    check(value) {
        
    }
}